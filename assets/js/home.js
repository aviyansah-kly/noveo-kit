const revealIO=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('show');revealIO.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>revealIO.observe(el));
const markerIO=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('marker-show');markerIO.unobserve(entry.target)}}),{threshold:.55});document.querySelectorAll('.highlight').forEach(el=>markerIO.observe(el));
const messages=[...document.querySelectorAll('.top-message')];let msgIndex=0;if(messages.length>1&&!window.matchMedia('(prefers-reduced-motion: reduce)').matches){setInterval(()=>{const current=messages[msgIndex];current.classList.remove('active');current.classList.add('exit');msgIndex=(msgIndex+1)%messages.length;const next=messages[msgIndex];next.classList.remove('exit');requestAnimationFrame(()=>next.classList.add('active'));setTimeout(()=>current.classList.remove('exit'),500)},3400)}

document.querySelectorAll('.mobile-nav-toggle').forEach(toggle=>{const header=toggle.closest('.header');const menu=header?.querySelector('.mobile-menu');if(!menu)return;const close=()=>{menu.classList.remove('open');toggle.setAttribute('aria-expanded','false')};toggle.addEventListener('click',()=>{const open=menu.classList.toggle('open');toggle.setAttribute('aria-expanded',open?'true':'false')});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',close));window.addEventListener('resize',()=>{if(window.innerWidth>980)close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})});
document.querySelectorAll('.foot-links a').forEach(link=>{const label=link.textContent.trim().toLowerCase();if(label==='privacy')link.setAttribute('href','privacy.html');if(label==='terms')link.setAttribute('href','terms.html')});
